<?xml version="1.0" encoding="utf-8"?>
<resources>

	
	<declare-styleable name="cp_input">
		<attr name="hint" format="string" />
		<attr name="keyText" format="string" />
		<attr name="background" format="reference" />
		<attr name="enable" format="boolean" />
		<attr name="inputType" format="string" />
		<attr name="textColor" format="color" />
		<attr name="height" format="dimension" />
		<attr name="width" format="dimension" />
		<attr name="maxLength" format="integer" />
		<attr name="isTip" format="boolean" />
		<!-- 输入框起始位置靠左 -->
		<attr name="keepLeft" format="boolean" />
		<attr name="gravity">
			<enum name="left" value="0x03" />
			<enum name="center_vertical" value="0x10" />
			<enum name="right" value="0x05" />
		</attr>
	</declare-styleable>
	<declare-styleable name="cp_rich_textview">
		<attr name="prefix" format="string" />
		<attr name="suffix" format="string" />
		<attr name="integerSize" format="dimension" />
		<attr name="decimalSize" format="dimension" />
		<attr name="text" format="string" />
		<attr name="paddingTopDecimal" format="dimension" />
	</declare-styleable>
	<declare-styleable name="PullToRefresh">

		<!-- A drawable to use as the background of the Refreshable View -->
		<attr name="ptrRefreshableViewBackground" format="reference|color" />

		<!-- A drawable to use as the background of the Header and Footer Loading Views -->
		<attr name="ptrHeaderBackground" format="reference|color" />

		<!-- Text Color of the Header and Footer Loading Views -->
		<attr name="ptrHeaderTextColor" format="reference|color" />

		<!-- Text Color of the Header and Footer Loading Views Sub Header -->
		<attr name="ptrHeaderSubTextColor" format="reference|color" />

		<!-- Mode of Pull-to-Refresh that should be used -->
		<attr name="ptrMode">
			<flag name="disabled" value="0x0" />
			<flag name="pullFromStart" value="0x1" />
			<flag name="pullFromEnd" value="0x2" />
			<flag name="both" value="0x3" />
			<flag name="manualOnly" value="0x4" />

			<!-- These last two are depreacted -->
			<flag name="pullDownFromTop" value="0x1" />
			<flag name="pullUpFromBottom" value="0x2" />
		</attr>

		<!-- Whether the Indicator overlay(s) should be used -->
		<attr name="ptrShowIndicator" format="reference|boolean" />

		<!-- Drawable to use as Loading Indicator. Changes both Header and Footer. -->
		<attr name="ptrDrawable" format="reference" />

		<!-- Drawable to use as Loading Indicator in the Header View. Overrides value set in ptrDrawable. -->
		<attr name="ptrDrawableStart" format="reference" />

		<!-- Drawable to use as Loading Indicator in the Footer View. Overrides value set in ptrDrawable. -->
		<attr name="ptrDrawableEnd" format="reference" />

		<!-- Whether Android's built-in Over Scroll should be utilised for Pull-to-Refresh. -->
		<attr name="ptrOverScroll" format="reference|boolean" />

		<!-- Base text color, typeface, size, and style for Header and Footer Loading Views -->
		<attr name="ptrHeaderTextAppearance" format="reference" />

		<!-- Base text color, typeface, size, and style for Header and Footer Loading Views Sub Header -->
		<attr name="ptrSubHeaderTextAppearance" format="reference" />

		<!-- Style of Animation should be used displayed when pulling. -->
		<attr name="ptrAnimationStyle">
			<flag name="rotate" value="0x0" />
			<flag name="flip" value="0x1" />
			<flag name="plain" value="0x2" />
			<flag name="regular" value="0x3" />
		</attr>

		<!-- Whether the user can scroll while the View is Refreshing -->
		<attr name="ptrScrollingWhileRefreshingEnabled" format="reference|boolean" />

		<!--
        	Whether PullToRefreshListView has it's extras enabled. This allows the user to be 
        	able to scroll while refreshing, and behaves better. It acheives this by adding
        	Header and/or Footer Views to the ListView.


		-->
		<attr name="ptrListViewExtrasEnabled" format="reference|boolean" />

		<!--
        	Whether the Drawable should be continually rotated as you pull. This only
        	takes effect when using the 'Rotate' Animation Style.

		-->
		<attr name="ptrRotateDrawableWhilePulling" format="reference|boolean" />

		<!-- BELOW HERE ARE DEPRECEATED. DO NOT USE. -->
		<attr name="ptrAdapterViewBackground" format="reference|color" />
		<attr name="ptrDrawableTop" format="reference" />
		<attr name="ptrDrawableBottom" format="reference" />
	</declare-styleable>
	
	<declare-styleable name="wheel_view">
		<attr name="centerbackground" format="reference|color" />
	</declare-styleable>
	<declare-styleable name="ServiceTelTextView">
        <attr name="beforeText" format="string" />
        <attr name="afterText" format="string" />
        <attr name="telNumber" format="string" />
    </declare-styleable>
    <declare-styleable name="FixedRatioRelativeLayout">
		<attr name="ratio" format="float"></attr>
		<attr name="adaptDensity" format="boolean"></attr>
	</declare-styleable>
	<declare-styleable name="GifView">
		<attr name="freezesAnimation" format="boolean" />
	</declare-styleable>

	<declare-styleable name="HorizontalRangeBar">
		<attr name="tickCount" format="integer" />
		<attr name="barColor" format="reference|color" />
		<attr name="connectingLineColor" format="reference|color" />
		<attr name="thumbRadius" format="dimension" />
		<attr name="thumbImageNormal" format="reference" />
		<attr name="thumbImagePressed" format="reference" />
		<attr name="thumbColorNormal" format="reference|color"/>
		<attr name="thumbColorPressed" format="reference|color"/>
	</declare-styleable>
</resources>